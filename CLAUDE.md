# منهاجي — تطبيق الطالب

> يعمل على هذا المستودع **مطوّران** بمساعدَين منفصلَين. هذا الملف هو ما يجعل
> النسختين تكتبان الشيفرة نفسها وتحترمان القرارات نفسها.

## قبل أي شغل — إلزامي

```bash
git pull --rebase          # صديقك قد يكون دفع شيئًا قبل دقائق
git log --oneline -10      # اقرأ ماذا تغيّر قبل أن تقترح
node test/run-all.js       # اعرف أنك تبدأ من أخضر
```

**لا تبدأ بتعديل قبل هذه الثلاثة.** أكثر ما يضيع الوقت هنا ليس التعارض في
الملفّات — بل أن يبني أحدهما شيئًا بناه الآخر بطريقة أخرى.

وإن كان التعديل أكبر من إصلاح صغير: **افتح فرعًا**.

## ما هو المشروع

تطبيق ويب للطالب السوري (بكالوريا وتاسع، ولاحقًا موادّ تقوية). **يعمل دون
إنترنت**: المحتوى يُسحب مرّةً ويُخزَّن على الجهاز، والشاشات تقرأ منه دائمًا لا
من الشبكة. الخادم Supabase، والفيديو على Cloudflare R2.

## قواعد الشيفرة

- **JavaScript صافٍ بلا أدوات بناء.** لا npm ولا حزم ولا خطوة ترجمة. الملفّات
  تُقرأ كما هي. لا تُدخل إطار عمل ولا مُجمِّعًا.
- **التعليقات بالعربية، وتشرح «لماذا» لا «ماذا».** الشيفرة تقول ماذا تفعل؛
  التعليق يقول لماذا اخترناها ولماذا رُفض البديل. تعليقٌ يعيد صياغة السطر
  ضجيجٌ يُحذف.
- بناء العناصر عبر `UI.h()` لا عبر `innerHTML` — إعادة الرسم بـinnerHTML تمسح
  ما كتبه الطالب في التمارين.
- كل نصّ يراه المستخدم بالعربية. النصّ اللاتيني داخل جملة عربية يمرّ عبر
  `UI.rich` وإلّا قفزت علامات الترقيم إلى الطرف الخطأ.

## قواعد لا تُكسر

**١. ارفع `VERSION` في `sw.js` مع كل تغيير يصل المستخدم.** بدونه يبقى
الطالب على النسخة القديمة إلى الأبد مهما نشرت.

**٢. الترقيم إلزامي عند قراءة أي جدول.** PostgREST يقصّ عند ١٠٠٠ صفّ **بلا
أي رسالة خطأ**. حدث هذا فعلًا: ٩٥ سؤالًا من ٤١١ وصلت الطلاب بلا خيارات
لأشهر. `Api.from()` تُرقّم تلقائيًا — لا تكتب طلبًا يدويًا يلتفّ حولها.

**٣. أي تعديل على سياسات RLS يحتاج إثباتًا قبل وبعد.** استعمل
`manhaji-backend/tools/probe-visibility-diff.mjs`: يلتقط ما يراه كل اشتراك صفًّا
بصفّ. هذا وصولٌ **مدفوع** — الخطأ يحجب محتوًى دُفع ثمنه أو يفتح ما لم يُدفع.

**٤. `js/data/seed.js` عقد الشكل لا مصدر بيانات.** لا تضع فيه محتوى تجريبيًا.
الشاشات تقرأ حقوله مباشرةً، و`applyStored` تُكمل منه ما ينقص المحتوى المحفوظ.

**٥. المنع في RLS يعود `200` بمصفوفة فارغة لا خطأً.** إن كتبت تحديثًا أو حذفًا
وتوقّعت نجاحه، اطلب `Prefer: return=representation` وافحص المصفوفة — وإلّا
قرأت المنع نجاحًا.

## قرارات مُتّخذة — لا تُنقض بلا نقاش

| القرار | لماذا |
|---|---|
| كل شيء بمعرّف **المادة** لا الكورس | طبقة الكورسات أُزيلت عمدًا. «التقوية» مادة إضافية لا طبقة بيع. |
| الفيديو على **R2** لا Supabase Storage | النقل مجاني. نقله إلى Supabase يقتل الكلفة عند ٢٥٠ طالب. |
| المحتوى في **IndexedDB** لا localStorage | حصّة localStorage ~٥ م.ب وتخزّن UTF-16 (بايتان للحرف العربي): الجدار عند ثلاث موادّ. |
| **لا شرائح بانر افتراضية** | بلا بانر مُضاف لا يُعرض شيء. البديل يشغل أبرز موضع بمحتوى لم يختره أحد. |
| **بصمة المحتوى** تسبق السحب | `content_version()` تُسأل أوّلًا؛ إن لم تتغيّر لا يُسحب الكتالوج. وفّرت ١٤٫٤ ثانية ← جزء من الثانية لكل فتحة. |
| السحب يعبر **الترقيم بالمفتاح** لا الإزاحة | `OFFSET` تحت RLS تُقيّم السياسة على كل صفّ تتخطّاه ثم ترميه. |

## الإطلاق التدريجي ومفتاح الإيقاف

الوصول بالأكواد، فالتدرّج مضبوط أصلًا بلا بناء إضافي: **أصدر ٢٠ كودًا أوّلًا،
راقب ٤٨ ساعة، ثم ١٠٠، ثم الكل.** غلطةٌ على عشرين مكالمتان؛ نفسها على ألفين
كارثة.

ما تراقبه في الـ٤٨ ساعة:
- صفحة **أعطال التطبيق** في اللوحة (تُجمَّع بالعطل لا بالوقت).
- **الفحص الدوري** في GitHub Actions — يعمل كل ٦ ساعات وبعد كل هجرة.

وإن ظهر عطل فادح: ارفع `halted` في جدول `app_config` من اللوحة. يرى كل طالب
شاشة «تحت الصيانة» برسالتك بدل تطبيق مكسور بلا تفسير — وهذا ليس ترفًا:
service worker يخدم النسخة المخزَّنة، فحتى النشر المصحَّح لا يصل الطالب إلّا
عند الفتحة التالية. الإيقاف **لا يلمس** RLS ولا الوصول المدفوع؛ هو رسالة في
الواجهة لا سياسة. وافتراضُه «لا إيقاف»، وفشلُ قراءته لا يوقف شيئًا — عطلٌ في
جدول الإعدادات يجب ألّا يحجب تطبيقًا سليمًا عمّن دفع ثمنه.

## الفحص

```bash
node test/run-all.js       # كل شيء
node test/smoke.js         # الأسرع
```

`test/suites/` مجموعات سلوكية تقود الشاشات الحقيقية لا نُسخًا منها.
`idb-check` تحتاج `npm i -D fake-indexeddb` وتُتخطّى بوضوح بدونها.

**الانضباط المتّبع — اتبعه:**

1. **أثبت الخلل أوّلًا** بفحصٍ يسقط على الشيفرة الحالية. فحصٌ يمرّ قبل الإصلاح
   لا يُثبت شيئًا.
2. أصلح.
3. أثبت أن الفحص صار يمرّ.
4. اترك الفحص حارسًا دائمًا.

**واحذر الفحص الذي ينجح بلا أن يفحص.** وقع هذا ثلاث مرّات في يوم واحد:
لقطةٌ كلّها أصفار، ومقارنةٌ بين الشيء ونفسه، وتحقّقٌ على مجموعة فارغة.
إن مرّ فحصٌ جديد من أوّل مرّة، **اجعله يسقط عمدًا** لتتأكّد أنه يرى شيئًا.

## أسلوب الكوميت

بالعربية. السطر الأوّل ما تغيّر، والجسم **لماذا** — وما الذي كان سيقع لولاه.
اذكر الأرقام إن قِست. لا تصف الشيفرة، فهي موجودة.

## مستودعات أخرى

- `manhaji-admin` — لوحة الإدارة (نفس القواعد)
- `manhaji-backend` — القاعدة والهجرات و`tools/probe-*.mjs`

المسابير تعمل على **الإنتاج**. تقرأ بأمان، وما يبذر منها يحذف ما بذره ويتحقّق.
